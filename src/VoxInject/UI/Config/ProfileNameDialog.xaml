<ui:FluentWindow x:Class="VoxInject.UI.Config.ProfileNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Nouveau profil"
        Width="320" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Nouveau profil"
                     ShowMaximize="False" ShowMinimize="False"/>

        <StackPanel Grid.Row="1" Margin="20,12,20,20">
            <TextBlock Text="Nom du profil" FontSize="12" Opacity="0.7"
                       Margin="0,0,0,6"/>
            <TextBox x:Name="NameBox" Margin="0,0,0,16"
                     KeyDown="NameBox_KeyDown"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="1" Content="Annuler"
                           Margin="0,0,8,0" Click="Cancel_Click"/>
                <ui:Button Grid.Column="2" Content="Créer"
                           Appearance="Primary"
                           Click="Ok_Click" IsDefault="True"/>
            </Grid>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
